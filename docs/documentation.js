/**
 * @module home
 * @title Firebase
 * @desc Firebase is Google's app development platform: analytics, authentication, cloud databases,
 * file storage, push messaging, remote configuration and crash reporting behind one project and one
 * set of credentials. This extension wraps the [Firebase C++ SDK](https://firebase.google.com/docs/cpp/setup)
 * for Android, iOS, Windows, macOS and Linux, and adds the three products that only exist as
 * platform SDKs - Crashlytics, Performance Monitoring and In-App Messaging - for Android and iOS.
 *
 * The package holds four extensions. `GMFirebase` carries every C++ SDK product and is the one every
 * project needs; `GMFirebaseCrashlytics`, `GMFirebasePerformance` and `GMFirebaseInAppMessaging` are
 * optional and only work alongside it.
 *
 * @section Setup
 * @desc Start here - the credentials, the extension options and the one call that comes first:
 * @ref page.getting_started
 * @ref page.extension_options
 * @ref page.platform_setup
 * @section_end
 *
 * @section Modules
 * @desc One module per Firebase product. ${module.app} covers what every module shares: initialisation,
 * the error contract and handles.
 *
 * @ref module.app
 * @ref module.analytics
 * @ref module.auth
 * @ref module.firestore
 * @ref module.database
 * @ref module.storage
 * @ref module.messaging
 * @ref module.remote_config
 * @ref module.functions
 * @ref module.app_check
 * @ref module.installations
 * @ref module.ump
 * @ref module.crashlytics
 * @ref module.performance
 * @ref module.in_app_messaging
 * @section_end
 *
 * @module_end
 */
